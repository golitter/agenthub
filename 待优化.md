# 待优化事项

> 临时跟踪文档：记录已识别、待排期的优化项。完成后移入对应设计文档并从此处删除。
>
> 2026-09-18：原第 1 项（Orchestrator 编排能力评测）、第 2.1/2.2 项与第 3 项工程卫生已实施完毕，移入 [docs/design/17-agent-eval-orchestration-capability.md](docs/design/17-agent-eval-orchestration-capability.md)。

---

## 1. Eval 待办（优先级：中）

1. **Agent 评委（Agent-as-a-Judge）原型**：给评委配只读工具（分页读 transcript / 读 diff 文件 / grep 事件），访问冻结快照；用 `rerun_graders` 在已冻结 trial 上离线 A/B 单发评委 vs agent 评委，数据说话后再决定是否转正。前置条件：确定性结构压缩已落地（`evals/transcript.py`）。
2. **编排能力在线配对实验**：实现已完成（见 design 17 §9），需在 eval 拓扑上实际执行 `--arm serial` / `--arm parallel` 批跑各 ≥3 次重复并跑 `make evals speedup`，回填验收清单（≥2 实现者并行 ✅ 已见 3 路并发、冲突恢复链 ❌ 本次未触发、speedup CI）。2026-09-18 单臂批跑基线：34/34 成功（修复 reason_node 400 后），总均分 96.81（`agentend/evals/tmp/batch-v21-full`）。
3. **冲突型 case 未真实触发 Git 冲突**：orch-conflict-001/002 两实现者扩展共享文件时调度时序天然串行化（max_concurrent 2/1，conflict_count=0），Resolver 恢复链未被测量。可考虑让任务规格强制两实现者同时改同一函数相邻区域，或提高并行度使改动真正相撞。

---

## 3. 工程卫生（优先级：低）

- 批跑残留目录（如曾经的 `backend/agentend/`）：批跑务必从 `agentend/` 目录启动或使用绝对 `--output`，防止相对路径在别处长出目录树。
- `evals/datasets/build_agenthub_agent_v2.py` 存量 25 处 E501（超长中文字符串行，与 HEAD 持平非新增），可在下次触碰该文件时顺手折行。
